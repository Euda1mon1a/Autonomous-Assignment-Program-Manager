# Core Framework
fastapi==0.127.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21

# Database
sqlalchemy==2.0.45
asyncpg==0.31.0
psycopg2-binary==2.9.11
alembic==1.17.2
sqlalchemy-utils==0.42.1
sqlalchemy-continuum==1.5.2  # Audit trail / version history for models

# Validation and serialization
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.3.0
jinja2==3.1.6  # Template rendering for notifications

# Authentication & Security
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.1,<6.0.0  # Pin bcrypt version for passlib 1.7.4 compatibility (prevents "password cannot be longer than 72 bytes" errors)
python-magic>=0.4.27  # File type detection for upload validation

# API Enhancements
fastapi-pagination==0.15.4
fastapi-cache2==0.2.2
slowapi==0.1.9
strawberry-graphql[fastapi]==0.287.3

# Data Processing
pandas==2.3.3
numpy==1.26.4
scipy==1.16.3  # Scientific computing (Sparse matrix & Erlang C)
python-dateutil==2.9.0.post0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0  # Model serialization

# Graph Analysis (for resilience hub vulnerability analysis)
networkx>=3.0
ndlib>=5.1.0  # Network diffusion library for burnout contagion modeling

# Cross-Disciplinary Engineering Analytics
# Libraries from manufacturing, epidemiology, and industrial engineering for resilience analysis
pyspc>=0.1.0  # Statistical Process Control charts with Western Electric rules
manufacturing>=1.2.0  # Six Sigma Cp/Cpk process capability analysis
ndlib>=5.1.0  # Network diffusion/epidemiology models (SIR on graphs)
pyworkforce>=0.2.0  # Erlang C queuing theory for workforce optimization

# Constraint Programming & Optimization
ortools>=9.8
pulp>=2.7.0
pyomo>=6.7.0  # Algebraic modeling + rich solution data capture (duals, slacks)

# Multi-Objective Optimization
pymoo>=0.6.0

# Quantum-Inspired Optimization (optional)
# Install these for quantum-inspired scheduling solvers:
# pip install pyqubo dwave-samplers
# For D-Wave quantum hardware access, also install:
# pip install dwave-system
# pyqubo>=1.0.0  # QUBO formulation library
# dwave-samplers>=1.0.0  # Classical simulated annealing (quantum-inspired)
# dwave-system>=1.0.0  # Uncomment for D-Wave quantum hardware access
# qubovert>=1.0.0  # Alternative QUBO library (optional)

# Monitoring & Observability
sentry-sdk[fastapi]==2.48.0
prometheus-fastapi-instrumentator==7.1.0

# OpenTelemetry - Distributed Tracing (pinned to 1.21.0/0.42b0 for compatibility)
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-instrumentation-fastapi==0.60b1
opentelemetry-instrumentation-sqlalchemy==0.60b1
opentelemetry-instrumentation-redis==0.60b1
opentelemetry-instrumentation-requests==0.60b1
opentelemetry-instrumentation-httpx==0.60b1

# OpenTelemetry Exporters (pinned for compatibility)
opentelemetry-exporter-otlp-proto-grpc==1.39.1  # OTLP gRPC exporter
opentelemetry-exporter-otlp-proto-http==1.39.1  # OTLP HTTP exporter
opentelemetry-exporter-jaeger==1.21.0  # Jaeger exporter (last version before deprecation)
opentelemetry-exporter-zipkin-json==1.39.1  # Zipkin exporter

# Reporting & Export
openpyxl==3.1.5
reportlab==4.4.7
python-docx==1.2.0
icalendar==6.3.2

# Background Tasks & Scheduling
celery==5.6.0
redis==7.1.0
apscheduler>=3.10.0  # Advanced job scheduling
croniter>=2.0.0  # Cron expression parsing

# Development Tools
black==25.12.0
ruff==0.14.10
mypy==1.19.1
isort==6.0.1

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
httpx==0.28.1
factory-boy==3.3.3
faker==38.2.0
freezegun==1.5.5
hypothesis==6.148.7

# Logging
loguru==0.7.3

# Cloud & Storage
boto3>=1.34.0

# Utilities
python-dotenv==1.2.1
python-decouple==3.8
watchdog==6.0.0  # File system monitoring for config hot reload

# Visualization
plotly>=5.18.0
kaleido>=0.2.1

# Game Theory (Axelrod's Prisoner's Dilemma tournaments)
axelrod>=4.12.0
