# Core Framework
fastapi==0.124.4
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.45
asyncpg==0.31.0
psycopg2-binary==2.9.11
alembic==1.17.2
sqlalchemy-utils==0.42.1
sqlalchemy-continuum==1.5.2  # Audit trail / version history for models

# Validation and serialization
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.3.0

# Authentication & Security
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.1,<5.0.0  # Pin bcrypt version for passlib 1.7.4 compatibility (prevents "password cannot be longer than 72 bytes" errors)
python-magic>=0.4.27  # File type detection for upload validation

# API Enhancements
fastapi-pagination==0.15.3
fastapi-cache2==0.2.2
slowapi==0.1.9
strawberry-graphql[fastapi]==0.219.2

# Data Processing
pandas==2.3.3
numpy==2.3.5
python-dateutil==2.9.0.post0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0  # Model serialization

# Graph Analysis (for resilience hub vulnerability analysis)
networkx>=3.0

# Constraint Programming & Optimization
ortools>=9.8
pulp>=2.7.0
pyomo>=6.7.0  # Algebraic modeling + rich solution data capture (duals, slacks)

# Multi-Objective Optimization
pymoo>=0.6.0

# Quantum-Inspired Optimization (optional)
# Install these for quantum-inspired scheduling solvers:
# pip install pyqubo dwave-samplers
# For D-Wave quantum hardware access, also install:
# pip install dwave-system
pyqubo>=1.4.0  # QUBO formulation library
dwave-samplers>=1.0.0  # Classical simulated annealing (quantum-inspired)
# dwave-system>=1.0.0  # Uncomment for D-Wave quantum hardware access
# qubovert>=1.0.0  # Alternative QUBO library (optional)

# Monitoring & Observability
sentry-sdk[fastapi]==2.48.0
prometheus-fastapi-instrumentator==7.1.0

# Reporting & Export
openpyxl==3.1.5
reportlab==4.4.6
python-docx==1.2.0
icalendar==6.1.0

# Background Tasks
celery==5.6.0
redis==7.1.0

# Development Tools
black==25.12.0
ruff==0.14.9
mypy==1.19.1

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
httpx==0.28.1
factory-boy==3.3.3
faker==38.2.0
freezegun==1.5.5
hypothesis==6.122.3

# Utilities
python-dotenv==1.2.1
python-decouple==3.8

# Visualization
plotly>=5.18.0
kaleido>=0.2.1
