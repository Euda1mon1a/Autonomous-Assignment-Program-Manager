# Archived Docker Configs

Production configs archived until deployment is needed.

## Files

- `docker-compose.prod.yml` - Production overrides (archived 2026-01-04)

## When Production is Needed

Clone `docker-compose.yml` and strip dev-only settings. Don't maintain two configs during active development.
